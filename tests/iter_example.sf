import iter

class Test
    val = 1 to 10
    count = 0

    fun __main__(self)
        return None
    
    fun __iter__(self)
        self.count = self.count + 1
        if self.count == 10
            return None
        return iter(self.val[self.count - 1])

x = Test()
for i in x
    write(i)