from std.dict import *

class Encoder
    fun __main__(self)
        return None

    fun encode(self, d)
        res = "{"
        for i, j in d
            res += '\"' + i + '\": ' + j.__str__() + ', '
        res = res[0 to len(res) - 2] + "}"
        return res